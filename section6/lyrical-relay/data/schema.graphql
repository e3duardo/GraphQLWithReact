schema {
  query: Root
}

type Root {
  songs: [Song]
  song(id: ID!): Song
}

type Song {
  id: ID
  title: String
  lyrics: [Lyric]
}

type Lyric {
  id: ID
  likes: Int
  content: String
  song: Song
}
